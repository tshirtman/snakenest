# you need a mongodb server
#sudo apt-get install mongodb

# optional, use virtualenv
virtualenv snakenest --no-dist-packages
source snakenest/bin/activate


pip install flask
pip install mongokit

setup your mongodb

python main.py

